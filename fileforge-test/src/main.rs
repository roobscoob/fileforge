use fileforge_lib::{diagnostic::{node::name::DiagnosticNodeName, pool::{entry::DiagnosticPoolEntry, DiagnosticPool}}, object::magic::Magic, provider::builtin::rust_slice::RustSliceBinaryProvider, reader::{endianness::Endianness, Reader}};

fn main() {
  // ideal byml api

  let byml = Byml::at(provider);

  byml.
}
